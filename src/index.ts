import { Selector } from './Selector';
import { DOM } from './Core';
import { domUtils } from './utils';

// const jDOM: typeof DOM.instance = DOM.instance;
// const jDOMplugin: typeof DOM.plugin = DOM.plugin;
// const jDOMcreate: typeof DOM.create = DOM.create;

export {
    Selector,
    DOM,
    domUtils
};
/**
export {
    Selector,
    DOM,
    domUtils,
    jDOM, 
    jDOMplugin, 
    jDOMcreate 
}; */

export default DOM;
